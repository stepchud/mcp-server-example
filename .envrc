use flake

# secrets specific to you live here
[[ -f $PWD/.envrc-personal ]] && source $PWD/.envrc-personal
# /specific secrets

# secrets live here
[[ -f $PWD/.vault_pass ]] && export ANSIBLE_VAULT_PASSWORD_FILE=$PWD/.vault_pass
# /secrets
