[project]
name = "mcp-server-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.60.0",
    "arxiv>=2.2.0",
    "dotenv>=0.9.9",
    "fastmcp>=2.10.6",
    "nest-asyncio>=1.6.0",
]
